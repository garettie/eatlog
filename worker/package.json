{
  "name": "eatlog-food-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "dry-run": "wrangler deploy --dry-run --config wrangler.subscription-staging.jsonc",
    "evaluate:estimates": "node scripts/evaluate-estimates.mjs",
    "smoke:health": "node scripts/smoke.mjs",
    "smoke:validation": "node scripts/smoke.mjs --validation"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260808.1",
    "@types/node": "^24.0.0",
    "tsx": "^4.23.1",
    "typescript": "~5.9.2",
    "wrangler": "^4.36.0"
  }
}
